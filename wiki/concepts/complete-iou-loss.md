---
name: complete-iou-loss
type: concept
sources: [yolov4]
updated: 2026-07-13
---

# CIoU Loss · 重叠、中心、长宽比三笔账一起算

## 一句话

框没盖好、中心偏了、形状不对，CIoU 都会分别处罚。

## 直觉

两个预测框可能有相同 IOU，却错得完全不同：一个只是整体向右偏，另一个中心正确但又高又窄。只看 IOU 分不出该往哪改。CIoU 把“往目标中心靠”和“把长宽比拧对”也写进损失，给优化器更明确的方向。

## 怎么做的

~~~text
L_CIoU = 1 - IoU + ρ²(b,b_gt)/c² + αv

v = 4/π² × [atan(w_gt/h_gt)-atan(w/h)]²
α = v / (1-IoU+v)
~~~

- <code>b,b_gt</code>：预测框与真值框中心。
- <code>ρ²</code>：两中心欧氏距离的平方。
- <code>c²</code>：两框最小外接框对角线平方，负责归一化距离。
- <code>w,h,w_gt,h_gt</code>：两框宽高。
- <code>v</code>：长宽比差异。
- <code>α</code>：让长宽比项随当前重叠情况自适应。

## 数字例子

真值中心 <code>(0,0)</code>、宽高 <code>4×2</code>；预测中心 <code>(1,0)</code>、宽高 <code>2×4</code>：

~~~text
交集=2×2=4，并集=8+8-4=12
IoU=4/12=0.33333 → 1-IoU=0.66667

ρ²=(1-0)²=1
最小外接框=4×4 → c²=4²+4²=32
中心项=1/32=0.03125

v=4/π²×[atan(2)-atan(0.5)]²≈0.16784
α=0.16784/(1-0.33333+0.16784)≈0.20110
αv≈0.03375

L_CIoU=0.66667+0.03125+0.03375≈0.73167
~~~

✓ 三项都能对上：覆盖不足 0.66667、中心偏移 0.03125、形状不合 0.03375。

## 跟其他框损失的对比

| 损失 | 重叠 | 不相交时仍有方向 | 中心距离 | 长宽比 |
|---|---:|---:|---:|---:|
| IoU | ✓ |  |  |  |
| GIoU | ✓ | ✓ |  |  |
| DIoU | ✓ | ✓ | ✓ |  |
| CIoU | ✓ | ✓ | ✓ | ✓ |

## 链接

- [[yolov4]] · 最终检测器使用 CIoU 框回归损失
- [[iou-intersection-over-union]] · 第一笔重叠账
- [[anchor-boxes]] · CIoU 优化 anchor 解码后的预测框
- [[bag-of-freebies-specials]] · 换损失不增加推理开销
